//hash loop
let number = "#";
while (number <= 7) {
console.log(number);
number = number + "#";
}
let hash = "#";
for (let hashNum = 0; hashNum < 7; hashNum++ ) {
    console.log(hash);
    hash += "#"
}